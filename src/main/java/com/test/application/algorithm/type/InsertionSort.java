package com.test.application.algorithm.type;

/**
 * 插入排序顾名思义，把最小或最大的元素插入到合适的位置直至遍历所有元素即可。也就是说插入排序要把第一个元素看作是有序的，从第二个元素开始和前一个元素比较大小，若符所有元素即可。
 * 也就是说插入排序要把第一个元素看作是有序的，从第二个元素开始和前一个元素比较大小，若符合条件则交换他们，重复这个过程直到不符合条件为止，这样一来这个元素之前的都是有序的，之后的都是无序的。
 * 重复这个过程直到最后一个元素比较完，排序就结束了。插入排序时间复杂度为O(n^2)
 */
public class InsertionSort {
}
