package com.test.application.algorithm.type;

/**
 * 快速排序
 * <p>
 * 在平均状况下，排序 n 个项目要Ο(nlogn) 次比较。
 * 在最坏状况下则需要Ο(n2) 次比较，但这种状况并不常见。事实上，快速排序通常明显比其他Ο(nlogn) 算法更快，因为它的内部循环(innerloop)可以在大部分的架构上很有效率地被实现出来。
 */
public class QuickSort {


    /**
     * 基本思想
     * <p>
     * 1.选定Pivot中心轴
     * 2.将大于Pivot的数字放在Pivot的右边
     * 3.将小于Pivot的数字放在Pivot左边
     * 4.分别对左右子序列重复前三步骤
     */
    private void quickSort() {


    }


}
